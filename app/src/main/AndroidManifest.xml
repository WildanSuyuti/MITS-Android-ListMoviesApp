<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mits.kakaroto.listmovieapp">
    <application
        android:name=".session.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".session.DashboardActivity"></meta-data>
        </activity>

        <activity
            android:name=".movie.UpdateMovieActivity"
            android:label="Update Movie">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".main.MainActivity"></meta-data>
        </activity>

        <activity
            android:name=".movie.AddMovieActivity"
            android:label="Add Movie"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".main.MainActivity"></meta-data>
        </activity>


        <activity android:name=".session.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".user.RegisterUserActivity"
            android:label="Register">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".session.DashboardActivity">
            </meta-data>

        </activity>

        <activity
            android:name=".session.DashboardActivity"
            android:label="Dashbord"></activity>

        <activity
            android:name=".user.UpdateUserActivity"
            android:label="Update User">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".session.DashboardActivity"></meta-data>
        </activity>

    </application>

</manifest>